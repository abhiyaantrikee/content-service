product: '1.0.0'

info:
  name: user-service
  title: user-service
  version: '1.0.0'

apis:
  'user-service':
    $ref: user-service.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
